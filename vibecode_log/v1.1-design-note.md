# v1.1 交互与显示优化设计笔记

> 第二阶段：在 v1.0 功能完整的基础上，提升整体界面的优雅度、交互触感与视觉层次。

## 阶段信息

- **阶段**：v1.1（Phase 2）
- **时间**：2026-08-06
- **目标**：不动大结构、不增加新页面，仅通过字体层级、动效、材质、微交互的优化，让现有界面更精致。
- **约束**：保持现有字体（Playfair Display / Inter / JetBrains Mono），保持 v1.1 色调（米白 + 暖棕墨 + 鎏金）。

## 参考 Skill

本次优化主要参考以下前端设计 skill 的指引：

- **apple-design**：用于字体层级（tracking/leading）、空间一致性、弹簧感曲线、减少运动偏好、材质深度。
- **emil-design-eng**：用于微交互决策、按钮按压反馈、hover 优化、自定义贝塞尔曲线、性能规则。
- **find-animation-opportunities**：扫描现有代码，找出哪些状态变化缺少过渡、哪些可按压元素缺少反馈。
- **improve-animations**：审计当前动画实现，给出优先级和精确值。

## 扫描发现（find-animation-opportunities）

### 待优化项

| # | 位置 | 当前问题 | 优化方向 | 频率/用途 |
|---|---|---|---|---|
| 1 | `ArticleCard.tsx:18`, `ProjectCard.tsx:13`, `ArchivePage.tsx:41`, `CategoriesPage.tsx:32`, `TagsPage.tsx:36` | `transition-all duration-300` 范围过大，且 hover 只有边框/阴影变化 | 改为具体属性过渡，加 `translateY(-2px)` + 更柔和阴影，按压加 `scale(0.98)` | 中频 / feedback |
| 2 | `SocialLinks.tsx:61` | 社交图标 hover 只有边框变色，缺少触感 | hover 加 lift + 金色 ring，active 加 `scale(0.95)` | 低频 / feedback |
| 3 | `Navbar.tsx:49` | Article 下拉菜单直接闪现 | 加淡入 + 轻微下移入场（`@starting-style`） | 中频 / preventing jarring change |
| 4 | `Navbar.tsx:45` | 导航链接 hover 只有颜色变化 | 加精致下划线动画（scaleX 从 0 → 1） | 中频 / feedback |
| 5 | `Layout.tsx:12` | 路由切换内容直接跳变 | 给 main 内容加淡入（fade-in） | 中频 / preventing jarring change |
| 6 | `HeroSection.tsx:11-21` | Hero 内容整体直接渲染 | 标题、副标题、社交按钮轻微错开入场（stagger 50-80ms） | 首屏 / delight |
| 7 | `StatsSection.tsx:33`, `MemoCard.tsx:17` | 统计卡片和 memo 卡片 hover 无反馈 | 加 hover lift + 阴影 | 低频 / feedback |
| 8 | `NotFoundPage.tsx:16` | 404 按钮无按压反馈 | 加 `active:scale(0.97)` | 低频 / feedback |

### 明确不做的项

- **键盘/高频导航动画**：Navbar 的 NavLink 点击属于高频操作，不加额外动画，只保留 hover 反馈。
- **文章列表 stagger**：文章列表滚动浏览时已经是可见状态，再 stagger 会显得拖沓，仅在首屏 Hero 使用。
- **复杂物理弹簧**：不引入 Framer Motion / Motion 等库，全部用 CSS transition 或 `@starting-style` 实现，保持项目轻量。

## 设计决策

### 1. 自定义缓动曲线

在 `src/index.css` 中新增 token：

```css
--ease-out: cubic-bezier(0.23, 1, 0.32, 1);
--ease-in-out: cubic-bezier(0.77, 0, 0.175, 1);
```

用于：
- `--ease-out`：按钮按压释放、卡片 hover 上浮、链接下划线
- `--ease-in-out`：下拉菜单、页面淡入

### 2. 动画时长

| 场景 | 时长 | 说明 |
|---|---|---|
| 按钮/卡片按压 | 100-160ms | 瞬间反馈 |
| hover 上浮 | 200-250ms | 自然跟随 |
| 链接下划线 | 300ms | 优雅但不过慢 |
| 下拉菜单入场 | 200ms | 轻快 |
| 页面淡入 | 200ms | 切换时不突兀 |
| Hero stagger | 50-80ms 间隔 | 首屏仪式感 |

### 3. 字体层级微调

- 大标题：`tracking-tight`（紧凑，显高级）
- 小标签/导航：`tracking-wide` 或 uppercase（增加辨识度）
- 正文：保持默认 tracking

### 4. 减少运动偏好

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

同时保留必要的颜色/透明度反馈（hover/active 颜色变化不依赖 duration 过长）。

### 5. 触摸设备 hover 保护

涉及 hover 的 transform 动画只在 `@media (hover: hover) and (pointer: fine)` 下生效，避免手机误触。

## 计划修改的文件

1. `src/index.css`
   - 新增 easing tokens
   - 新增 reduced motion 全局规则
   - 新增页面淡入、stagger 入场动画 keyframes
   - 调整 prose 标题 letter-spacing

2. `src/components/ArticleCard.tsx`
   - 优化 hover/active 交互

3. `src/components/ProjectCard.tsx`
   - 优化 hover/active 交互

4. `src/components/SocialLinks.tsx`
   - 优化图标按钮 hover/active

5. `src/components/Navbar.tsx`
   - 下拉菜单淡入
   - 导航链接下划线动画
   - 图标按钮按压反馈

6. `src/components/Layout.tsx`
   - 路由切换淡入

7. `src/components/HeroSection.tsx`
   - stagger 入场类名

8. `src/components/StatsSection.tsx`
   - 统计卡片 hover 反馈

9. `src/pages/ArchivePage.tsx`
   - 归档条目 hover/active

10. `src/pages/CategoriesPage.tsx` / `TagsPage.tsx`
    - 分类/标签卡片 hover/active

11. `src/pages/NotFoundPage.tsx`
    - 按钮按压反馈

## 验证标准

- `npm run build` 无错误
- 首页、文章列表、文章详情、分类、标签、归档、项目、memo、about、404 页面视觉正常
- 暗色模式下无突兀变化
- DevTools 开启 `prefers-reduced-motion: reduce` 后，位移动画消失，颜色反馈保留
- DevTools 慢速播放检查无抖动/跳跃

## 三角钢琴 3D 动画

- **实现方式**：程序化建模（方案 A）
- **技术栈**：Three.js + React Three Fiber + @react-three/drei
- **文件**：`src/components/PianoScene.tsx`
- **集成**：`src/components/HeroSection.tsx` 中用 `React.lazy` 懒加载，带占位 fallback
- **特性**：
  - 高光黑漆琴身 + 金色金属件 + 象牙白键
  - 缓慢漂浮动画（上下 + 轻微旋转）
  - 暗色模式下金色反光更明显
  - 纯装饰，无按键交互
- **优化**：按路由拆分为独立 chunk，避免首屏加载过多

## 后续

基础版钢琴已上线。后续可根据反馈优化：更精致的外形、更真实的材质、更戏剧的相机角度、或加入鼠标视差等。
